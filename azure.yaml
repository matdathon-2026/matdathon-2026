# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: didimheart
metadata:
  template: didimheart@0.1.0

services:
  app:
    language: dotnet
    project: ./infra/DidimHeart.AppHost/DidimHeart.AppHost.csproj
    host: containerapp
